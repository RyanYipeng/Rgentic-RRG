====================================================================
                   MCP RAG 项目 - 精简版
====================================================================

📁 项目结构
--------------------------------------------------------------------
Rgentic RRG/
├── server.py                  # MCP 服务器（用于 Cursor/Claude）
├── rag_app/                   # RAG 核心模块
│   ├── __init__.py
│   ├── data.py               # Embedding & FAQ 数据
│   ├── vdb.py                # Qdrant 向量数据库
│   └── retriever.py          # 检索器
├── test_basic.py             # 测试1: 本地直接测试 ⭐推荐新手
├── test_llamaindex.py        # 测试2: LlamaIndex Agent 测试
├── test_server.py            # 测试3: MCP 服务器测试
├── requirements.txt          # 所有依赖（已整合）
├── .env.example              # 环境变量模板
├── mcp.json                  # MCP 配置文件
└── README.md                 # 详细文档

====================================================================
🎯 三种测试方式
====================================================================

┌─────────────────────────────────────────────────────────────────┐
│ 测试1: 本地直接测试（最简单）                                    │
├─────────────────────────────────────────────────────────────────┤
│ 命令: .venv\Scripts\python test_basic.py                        │
│ 功能: - 测试向量检索                                             │
│       - 测试 Web 搜索                                            │
│       - 配合 Ollama 进行 RAG 问答                               │
│ 特点: 不需要启动 server.py，不需要 LlamaIndex                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ 测试2: LlamaIndex Agent（智能）                                 │
├─────────────────────────────────────────────────────────────────┤
│ 命令: .venv\Scripts\python test_llamaindex.py                   │
│ 功能: - Agent 自动选择工具                                      │
│       - 智能问答流程                                             │
│ 特点: 需要 LlamaIndex，不需要启动 server.py                     │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ 测试3: MCP 服务器（跨应用）                                     │
├─────────────────────────────────────────────────────────────────┤
│ 命令: .venv\Scripts\python test_server.py                       │
│ 功能: - 启动 MCP 服务器                                         │
│       - 提供标准 MCP 协议接口                                   │
│       - 可被 Cursor/Claude Desktop 调用                         │
│ 特点: 需要启动 server.py，用于跨应用集成                        │
└─────────────────────────────────────────────────────────────────┘

====================================================================
⚡ 快速开始
====================================================================

1. 安装依赖
   pip install -r requirements.txt

2. 配置环境（可选）
   copy .env.example .env
   # 编辑 .env 添加 SERPER_API_KEY（用于网络搜索）

3. 选择测试方式
   .venv\Scripts\python test_basic.py        # 最简单

====================================================================
💡 使用场景决策
====================================================================

┌──────────────────┬───────────────┬─────────────────────┐
│ 你的需求         │ 需要 server.py│ 推荐测试脚本         │
├──────────────────┼───────────────┼─────────────────────┤
│ 本地测试验证     │ ❌            │ test_basic.py       │
│ 开发调试         │ ❌            │ test_basic.py       │
│ 智能 Agent 问答  │ ❌            │ test_llamaindex.py  │
│ 集成到 Cursor    │ ✅            │ test_server.py      │
│ 集成到 Claude    │ ✅            │ test_server.py      │
│ 自己的 Python 应用│ ❌           │ 直接导入 rag_app    │
└──────────────────┴───────────────┴─────────────────────┘

====================================================================
🔧 在自己应用中使用
====================================================================

from rag_app import Retriever, QdrantVDB, EmbedData

# 初始化
embed = EmbedData()
vdb = QdrantVDB(collection="my_collection", vector_size=embed.dim)
retriever = Retriever(vdb, embed)

# 使用
result = retriever.search("你的问题", k=3)

====================================================================
📦 依赖说明
====================================================================

核心依赖（必须）:
  - mcp                    # MCP 协议支持
  - qdrant-client          # 向量数据库
  - sentence-transformers  # 向量化模型
  - python-dotenv          # 环境变量
  - requests               # HTTP 请求

可选依赖:
  - llama-index            # 仅 test_llamaindex.py 需要
  - llama-index-llms-ollama

外部工具（可选）:
  - Ollama                 # 本地 LLM（用于测试）
  - Node.js                # MCP Inspector（调试工具）

====================================================================
✨ 项目特点
====================================================================

✅ 结构清晰：3个测试文件，各司其职
✅ 依赖整合：所有依赖在 requirements.txt
✅ 灵活使用：可独立使用，也可作为 MCP 服务
✅ 文档完善：README.md 提供详细说明
✅ 易于扩展：修改 rag_app/data.py 添加数据

====================================================================
