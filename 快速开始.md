# 🚀 快速开始指南

## 📋 项目概览

```
MCP RAG 项目 = 向量检索 + 网络搜索 + MCP 协议
用途: 本地测试 | LlamaIndex 集成 | Cursor/Claude 集成
```

## ⚡ 三步开始

### 1️⃣ 安装依赖
```bash
pip install -r requirements.txt
```

### 2️⃣ 选择测试方式
```bash
# 方式1: 最简单（推荐）
.venv\Scripts\python test_basic.py

# 方式2: LlamaIndex Agent
.venv\Scripts\python test_llamaindex.py

# 方式3: MCP 服务器
.venv\Scripts\python test_server.py
```

### 3️⃣ （可选）配置环境
```bash
copy .env.example .env
# 编辑 .env 添加 SERPER_API_KEY
```

## 📊 测试对比

| 测试 | 复杂度 | 需要 Ollama | 需要 LlamaIndex | 需要 server.py |
|------|--------|------------|----------------|---------------|
| test_basic.py | ⭐ | 可选 | ❌ | ❌ |
| test_llamaindex.py | ⭐⭐ | ✅ | ✅ | ❌ |
| test_server.py | ⭐⭐⭐ | ❌ | ❌ | ✅ |

## 💡 我应该用哪个？

**新手/快速测试** → `test_basic.py`
- 最简单，5分钟看到效果
- 测试所有核心功能

**需要智能 Agent** → `test_llamaindex.py`
- Agent 自动选择工具
- 更复杂的对话流程

**集成到 Cursor/Claude** → `test_server.py`
- 启动 MCP 服务器
- 跨应用调用工具

## 🎯 常见场景

### 场景1: 我只想快速测试
```bash
.venv\Scripts\python test_basic.py
```

### 场景2: 我要在 Cursor 中使用
```bash
# 1. 配置 mcp.json（见 README.md）
# 2. 启动服务器
.venv\Scripts\python test_server.py
```

### 场景3: 我要集成到自己的应用
```python
from rag_app import Retriever, QdrantVDB, EmbedData

embed = EmbedData()
vdb = QdrantVDB(collection="my_db", vector_size=embed.dim)
retriever = Retriever(vdb, embed)

result = retriever.search("问题", k=3)
```

## ✅ 验证安装

运行基础测试（30秒内完成）：
```bash
.venv\Scripts\python test_basic.py
```

看到 `✅ 测试完成` 即成功！

## 📚 详细文档

- 完整说明：`README.md`
- 项目结构：`项目说明.txt`
